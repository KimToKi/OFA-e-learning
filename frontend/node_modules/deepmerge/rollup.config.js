import resolve from '@rollup/plugin-node-resolve'; // ใช้แพ็กเกจใหม่
import commonjs from '@rollup/plugin-commonjs'; // ใช้แพ็กเกจใหม่
import babel from '@rollup/plugin-babel'; // ใช้ Babel plugin
import pkg from './package.json';

export default {
  input: 'index.js',
  plugins: [
    commonjs(),
    resolve({
      extensions: ['.js', '.jsx'], // รองรับ .jsx
    }),
    babel({
      exclude: 'node_modules/**', // ไม่คอมไพล์ไฟล์จาก node_modules
      presets: ['@babel/preset-env', '@babel/preset-react'], // คอมไพล์ JSX
    }),
  ],
  output: [
    {
      file: pkg.main,
      format: 'cjs',
    },
    {
      name: 'deepmerge',
      file: 'dist/umd.js',
      format: 'umd',
    },
  ],
};
